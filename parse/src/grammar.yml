TOKENS:
  - mlComment:
    regex: /\/\*[\s\S]*\*\//
    group: SKIPPED
  - comment:
    regex: /\/\/.*/
    group: SKIPPED
  - ws:
    regex: /\s+/
    group: SKIPPED
  - string:
    regex: /"[^"]*"/

RULES:
  statements:
    - mlComment
    - comments
